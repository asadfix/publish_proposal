% ============================================================
% 07_cost.tex
% ============================================================

\section{Step 6: The Cost of Assuming Equivalence}
\label{sec:cost}

The final step prices the assumption the field currently makes for free. From the same corpus we construct two indices. The naive index pools posts across platforms, the construction canonical since \citet{oconnor2010}; the dissertation's version was a daily mean of scored posts (Figure~\ref{fig:naive}), and Step 6 constructs both that variant and the share-negative variant. The adjusted index applies the platform-specific measurement parameters from Step 3 and, where the fork in Section~\ref{sec:linking} permits, the linking functions from Step 5. We compare the two on the quantities an analyst would actually report: index levels, trends, and the estimated sentiment shift around each anchor event, including sign flips in that shift. Both indices are also fed to a Prophet forecasting model, mirroring the dissertation's forecasting stage, which applied Prophet to a smoothed daily sentiment index and decomposed it into trend and seasonal components (Figure~\ref{fig:seasonal}); the forecast comparison is a downstream-consequences illustration only, since a biased but stable index can forecast well, and the primary loss is index levels, event shifts, and sign flips. Because Steps 2, 3 and 5 estimate parameters within anchor windows, applying them to the full corpus is an extrapolation; we report a sensitivity analysis excluding the Other/Unclear category, which never enters an estimation window. \todo{Specify how the Step 3 parameters become an index: the current plan is platform-specific latent means under the retained partial-invariance constraints, aggregated with fixed platform weights; confirm.} Finally, we simulate composition change: reweighting platform shares to mimic a platform losing API access \citep{davidson2023, goanta2026} shows how much apparent sentiment movement is a measurement and composition artefact rather than opinion change. Label-error corrections do not touch this quantity, because the error lies between platforms, not between classifier and criterion \citep{egami2023}.

\begin{figure}[t]
\centering
\includegraphics[width=0.8\linewidth]{fig_prophet_seasonal}
\caption{Dissertation-stage Prophet decomposition of the pooled index (dissertation Figure~7.6, p.~39): a declining long-run trend and a midweek dip that Step~6 treats as a candidate composition artefact, consistent with weekday differences in platform mix as much as with mood. If Step~3 finds non-invariance, components like these inherit the measurement artefacts of the pooled construction.}
\label{fig:seasonal}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{fig_prophet_naive_index}
\caption{The naive pooled index in practice: dissertation-stage daily mean sentiment pooled across platforms, with the Prophet fit and forecast band (dissertation Figure~7.7, p.~39). The figure exhibits the construction's pathologies at a glance: days with a single post pin the daily mean at plus or minus one, no support threshold or volume weighting is applied, the uncertainty band ignores the bounded scale, and the axis spans 2010 to 2026, one reason the collection window is under recount. Step~6 prices this construction against a measurement-adjusted alternative.}
\label{fig:naive}
\end{figure}

\blocked{Results: naive versus adjusted index comparison, event-shift bias, sign flips, forecast error, and composition simulation.}
