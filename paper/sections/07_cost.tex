% ============================================================
% 07_cost.tex
% ============================================================

\section{Step 6: The Cost of Assuming Equivalence}
\label{sec:cost}

The final step prices the assumption the field currently makes for free. From the same corpus we construct two indices. The naive index pools posts across platforms and tracks the share of negative posts over time, the construction canonical since \citet{oconnor2010}; the dissertation itself built exactly such an index (Figure~\ref{fig:naive}). The adjusted index applies the platform-specific measurement parameters from Step 3 and, where the fork in Section~\ref{sec:linking} permits, the linking functions from Step 5. We compare the two on the quantities an analyst would actually report: index levels, trends, and the estimated sentiment shift around each anchor event, including sign flips in that shift. Both indices are also fed to a Prophet forecasting model, mirroring the dissertation's forecasting stage, which applied Prophet to a smoothed daily sentiment index, and forecast error is compared. Finally, we simulate composition change: reweighting platform shares to mimic a platform losing API access \citep{davidson2023, goanta2026} shows how much apparent sentiment movement is a measurement and composition artifact rather than opinion change. Label-error corrections do not touch this quantity, because the error lies between platforms, not between classifier and criterion \citep{egami2023}.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{fig_prophet_naive_index}
\caption{The naive pooled index in practice: dissertation-stage daily mean sentiment pooled across platforms, with the Prophet fit and forecast band (dissertation Figure~7.7, p.~39). The horizontal axis spans 2010 to 2026, one reason the collection window is under recount. Step~6 prices exactly this construction against a measurement-adjusted alternative.}
\label{fig:naive}
\end{figure}

\blocked{Results: naive versus adjusted index comparison, event-shift bias, sign flips, forecast error, and composition simulation.}
